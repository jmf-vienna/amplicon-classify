if (interactive()) {
  require(conflicted)

  require(targets)

  require(cli)
  require(dplyr)
  require(forcats)
  require(fs)
  require(ggplot2)
  require(purrr)
  require(readr)
  require(rlang)
  require(stringr)
  require(tibble)
  require(tidyr)
  require(vctrs)

  tar_load_globals()

  options(warn = 1L)
}
